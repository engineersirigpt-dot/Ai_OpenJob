# สรุปสิ่งที่ทำ

เอกสารนี้สรุปสถานะงานล่าสุดของระบบ `AI Meeting Assistant` ในส่วนการบันทึกการประชุมและประวัติการประชุม

## ที่เก็บข้อมูล

ระบบใช้ `SQLite` ที่มากับ `Node 25` ผ่าน `node:sqlite`

- เก็บข้อมูลไว้ในไฟล์ `data/meetings.db` ภายในโปรเจค
- ไม่ต้องลง package เพิ่ม
- ไม่ต้อง compile database driver
- ไม่ต้องต่อ `SQL Server`

## การทำงานหลัก

เมื่อกด `จบประชุม` ระบบจะทำงานตามลำดับนี้

1. เก็บ `Job ID` ที่ร่วมประชุม โดยดึงจากเอกสารที่โหลดเข้าในรอบประชุมนั้น
2. เก็บหัวข้อประชุม
3. เก็บเวลาเริ่มและเวลาจบ
4. คำนวณระยะเวลาประชุม
5. เก็บ `transcript` เต็มของการประชุม
6. ให้ AI สรุปการประชุมอัตโนมัติด้วย `gpt-4o-mini`
7. บันทึกข้อมูลทั้งหมดลงฐานข้อมูล
8. แสดงสถานะ `บันทึกแล้ว · ดูประวัติ`

## สิ่งที่ AI สรุปให้

AI จะสรุปผลประชุมออกมาเป็นหัวข้อหลักดังนี้

- ประเด็นหลักที่คุยกัน
- ข้อสรุปหรือผลตัดสินใจ
- `action items` หรือสิ่งที่ต้องทำต่อ

## แท็บประวัติ

มีแท็บ `ประวัติ` สำหรับดูการประชุมย้อนหลัง โดยมีความสามารถดังนี้

- แสดงรายการประชุมย้อนหลัง
- แสดงหัวข้อประชุม
- แสดงป้าย `Job ID`
- แสดงเวลา
- แสดงระยะเวลา
- คลิกเข้าไปดูสรุปการประชุมได้
- คลิกเข้าไปดูบทสนทนาเต็มได้
- ลบประวัติการประชุมได้

## สรุปสั้น

ตอนนี้ระบบรองรับ flow หลังประชุมครบแล้ว คือ

- บันทึกข้อมูลประชุมลง `SQLite`
- สรุปการประชุมอัตโนมัติด้วย AI
- เปิดดูประวัติย้อนหลังได้
- ดู transcript เต็มได้
- ลบประวัติได้

## แบ่งงานต่อ

ถ้าจะเดินงานต่อจากจุดนี้ แนะนำให้แบ่งงาน 3 คนดังนี้

### ทิม

ดูแล flow หลักฝั่งหน้าใช้งาน

- ตรวจ flow ตั้งแต่เริ่มประชุมจนถึงกด `จบประชุม`
- เช็กการแสดงสถานะ `บันทึกแล้ว · ดูประวัติ`
- เก็บ UX ระหว่างบันทึก เช่น loading, success, error
- ตรวจว่าการดึง `Job ID` จากเอกสารที่โหลดมาถูกต้องทุกกรณี
- ทดสอบการเปิดดูประวัติหลังบันทึกทันที

### ทิว

ดูแลฝั่ง backend และฐานข้อมูล

- ดูแล `node:sqlite` และไฟล์ `data/meetings.db`
- ตรวจ schema และความถูกต้องของข้อมูลที่บันทึก
- เช็ก API ประวัติประชุม เช่น list, detail, delete
- ตรวจกรณี transcript ว่างหรือข้อมูลไม่ครบ
- เช็กกรณี AI สรุปไม่สำเร็จว่าระบบยังบันทึกข้อมูลประชุมได้

### เบค

ดูแลเอกสาร, QA, และการตรวจงานรวม

- อัปเดต `README.md` ให้ตรงกับ flow ล่าสุด
- อัปเดตคำอธิบายการใช้งานแท็บประวัติ
- ทำ checklist ทดสอบการบันทึกและดูประวัติ
- ทดสอบการลบประวัติการประชุม
- รวบรวม issue จากการใช้งานจริงแล้วส่งกลับให้ทิมกับทิวแก้
